---
import { KNOWN_LANGUAGES } from '../../consts';

export const getStaticPaths = () => Object.keys(KNOWN_LANGUAGES).map((lang) => ({ params: { lang } }));

const { lang } = Astro.params; 
---

<meta http-equiv="refresh" content={`0;url=/${lang}/introduction/`} />
