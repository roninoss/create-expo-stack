/* eslint-env node */
const { defineConfig } = require('eslint/config');
const expoConfig = require('eslint-config-expo/flat');

module.exports = defineConfig([
  expoConfig,
  {
    ignores: ['dist/*'],
  },
  {
    rules: {
      'react/display-name': 'off',
      // Disable unresolved import errors for Convex generated files
      // These files are created when running 'convex dev'
      'import/no-unresolved': ['error', { ignore: ['\\.\\/\\_generated\\/'] }],
    },
  },
]);