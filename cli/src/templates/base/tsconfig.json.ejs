{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  <% if (props.navigationPackage?.name === 'expo-router') { %>
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
  <% } else { %>
    }
  <% } %>
}
