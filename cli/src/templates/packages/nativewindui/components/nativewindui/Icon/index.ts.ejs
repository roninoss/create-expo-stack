import { cssInterop } from 'nativewind';

import { Icon } from './Icon';

cssInterop(Icon, {
  className: {
    target: 'style',
    nativeStyleToProp: {
      color: 'color',
      height: 'size',
      width: 'size',
    },
  },
});

export { Icon };